dist: xenial

language: python

python:
  - 3.7

install:
  - pip install -r requirements.txt
  - pip install -r requirements-dev.txt

script: yarn test

notifications:
  email:
    on_success: change
    on_failure: change